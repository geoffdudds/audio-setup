This repo contains the configuration files for raspotify and snapcast with two rpi's, both driving a khadas toneboard DAC.

steps
- install raspotify, snapserver, and snapclient on server
- install snapclient on client
- copy conf files etc for both
